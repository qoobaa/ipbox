(function () {
  var entryNode = document.getElementById("entry-<%= @entry.id %>")

  if (entryNode) {
    entryNode.innerHTML = "<%= j render(@entry) %>";
  } else {
    Turbolinks.visit(window.location);
  }
})();
